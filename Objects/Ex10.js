[1, 2, ["a", ["b", false]], null, {}]

// list primitive values and objects

// primitives: (6)
  // 1, 2, 'a', 'b', false, null

// objects: (4)
  // whole outer array
  // the ['b', false] array
  // the ['a', ['b', false]] array
  // the empty object at index 4