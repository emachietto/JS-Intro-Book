// no error, but no modification to i in initialization clause
// i thought it would only log 1
  // incrementing in a log statement was weird to me
    // but it logs the return value and also increments i
// it takes the reassignment to the conditional and continues running