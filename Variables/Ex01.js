let name = 'Victor';

function greeter(name) {
  console.log(`Good Morning, ${name}.`);
  console.log(`Good Afternoon, ${name}.`);
  console.log(`Good Evening, ${name}.`);
}

greeter(name);

// aaaand they just wanted console.log statements